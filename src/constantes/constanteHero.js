// ─── Constantes de tilt del logo (hero) ───────────────────────────────────────
const TILT = {
  MOUSE_MAX_DEG: 14,
  ORIENT_MAX_DEG: 42,
  ORIENT_SENS_BETA: 1.15, // adelante/atrás  → rotationX
  ORIENT_SENS_GAMMA: 1.35, // izquierda/dcha  → rotationY
  ORIENT_QUICK_TO_SEC: 0.16,
}

export { TILT }
